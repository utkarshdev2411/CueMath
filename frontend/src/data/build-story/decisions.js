// Architectural and product decisions made while building the Cuemath AI Tutor Screener.
// Each entry captures: what I chose, what I rejected, and what I'd change with budget.

export const DECISIONS = [
  {
    id: "groq-vs-openai",
    decision: "LLM provider: Groq (llama-3.3-70b-versatile)",
    context: "Need a fast, capable LLM for both real-time interview replies (must respond in <1.5s to feel conversational) and structured JSON assessment. Budget: ₹0.",
    options_considered: ["Groq (llama-3.3-70b)", "OpenAI GPT-4o-mini", "Anthropic Claude 3.5 Haiku", "Self-hosted Llama via Ollama"],
    chosen: "Groq (llama-3.3-70b)",
    reasoning: "Groq's free tier (30 RPM, 6000 TPM, 1000 RPD) is generous enough for the demo. Output speed is 200–500 tokens/second — five times faster than OpenAI on the same model class — which matters because every 200ms of latency in a voice interview is a 200ms uncomfortable silence. The OpenAI-compatible endpoint means I use the openai Python SDK with just a base_url override, so swapping providers later is a one-line change.",
    tradeoff: "Free-tier means rate limits are real. If two candidates hit the system in the same minute the second one risks a 429. Also the model isn't quite as good as GPT-4 at JSON adherence — needed retry logic for malformed responses.",
    production_path: "Move to Groq paid tier or Azure OpenAI for SLAs. Add a cheap router that falls back to OpenAI if Groq returns 429. The provider boundary already exists in groq_client.py — swapping is a one-day job.",
  },
  {
    id: "speech-recognition-stack",
    decision: "Speech-to-text: Browser Web Speech API",
    context: "Candidate speech needs to be transcribed in real time, with low latency, reliably enough that a non-technical tutor can complete a 5-minute interview without a single re-attempt.",
    options_considered: ["Web Speech API (Chrome)", "OpenAI Whisper (hosted)", "AssemblyAI streaming", "Groq Whisper (whisper-large-v3)"],
    chosen: "Web Speech API",
    reasoning: "Zero cost, zero latency added on top of Google's recognition (which is excellent for Indian English), and zero backend complexity — no audio bytes ever leave the browser. The Chrome-only constraint is a real cost, but it's a cost I can disclose upfront with browser detection on the landing page. Whisper-class APIs would need MediaRecorder, chunked uploads, server-side VAD and a WebSocket — that's a 2-day rewrite for a feature the candidate cannot see.",
    tradeoff: "Chrome-desktop only. No control over the audio pipeline (Hush noise suppression for example cannot be injected). Network blips on Chrome's audio sandbox can produce confusing 'network' errors that I had to handle explicitly with backoff.",
    production_path: "If Cuemath actually scales this, swap to Groq Whisper + a server-side VAD. That unlocks Firefox/Safari/Edge, lets us drop in Hush for noise suppression, and gives us control over the full audio pipeline. The work is specced in detail in docs/hush-model-analysis.md.",
  },
  {
    id: "stateless-vs-stateful",
    decision: "Stateless backend (no DB, no session)",
    context: "The backend has to handle real interviews and an admin dashboard. The natural assumption is to add Postgres or a key-value store.",
    options_considered: ["Stateless (client owns transcript)", "Postgres + SQLAlchemy", "Supabase", "Redis for ephemeral session state"],
    chosen: "Stateless (client owns transcript)",
    reasoning: "Every /api/chat request already carries the last 4 messages, and /api/assess takes the full transcript in one shot. The backend has no reason to remember anything between calls. No DB means no schema migrations, no connection pool, no auth, and a one-file backend that's easy to reason about. Statelessness is also philosophically correct here: transcripts belong to the candidate and Cuemath, not to my random demo backend.",
    tradeoff: "Reports vanish if the candidate clears localStorage. No analytics, no historical data, no admin auth. Anyone with the URL can hit the API.",
    production_path: "Add Postgres for interview records + Supabase auth for the admin dashboard. The backend stays stateless from the LLM's perspective — DB is just a write-through log of what the client sent. Roughly a 2-day addition.",
  },
  {
    id: "seven-turn-limit",
    decision: "Hard 7-turn limit, enforced server-side",
    context: "Need to balance assessment quality against candidate fatigue, Groq token budget, and total interview time.",
    options_considered: ["5 turns (3 minutes)", "7 turns (5 minutes)", "10 turns (8 minutes)", "Open-ended with a stop button"],
    chosen: "7 turns (5 minutes)",
    reasoning: "5 turns isn't enough to cover all 5 rubric dimensions with one purpose-built question each (warm opener doesn't score anything, close doesn't score anything → 5 scoring turns required). 10+ turns burns candidate patience and Groq tokens for diminishing return. 7 is the sweet spot: opener + 5 dimension probes + warm close, all under 5 minutes. Backend enforces the limit (turn_count > 7 returns HTTP 400) so frontend bugs can't accidentally extend interviews.",
    tradeoff: "If a candidate is rambling on turn 3, you can't gracefully cut them short — the 7 turn structure is rigid. Some great candidates would benefit from a longer probe.",
    production_path: "Make turn count configurable per role (junior tutor = 5 turns, senior tutor = 9 turns). Add a 'depth' parameter that controls how aggressively Priya probes for STAR examples on each turn.",
  },
  {
    id: "non-continuous-recognition",
    decision: "Hybrid speech recognition: continuous=true with manual silence detection",
    context: "Naive continuous=true accumulates transcripts across turns (bug documented in known-gotchas.md). Naive continuous=false auto-stops after ~2s of silence which is too short for natural Indian-English speech pauses.",
    options_considered: ["Pure continuous=false (auto-stop)", "continuous=true with 4s silence timer", "continuous=false but restart on every onend"],
    chosen: "continuous=true with 4s silence timer",
    reasoning: "I tried continuous=false first because the docs and rules say so. It auto-stopped after 2s — candidates would pause to think and the system would submit half-formed answers. Switching to continuous=true with a manual 4s SUBMIT_SILENCE_MS fixed it: the buffer accumulates isFinal chunks, and submission fires only after 4 full seconds of silence. The trick is that I destroy the SpeechRecognition instance after each turn, so transcripts never bleed across turns the way the gotcha doc warns about.",
    tradeoff: "Slightly more complex than the textbook pattern. Mid-sentence pauses over 4s do submit the answer prematurely (rare but possible). The .cursorrules file even tells me to never set continuous=true — I went against that rule deliberately because the textbook pattern was producing broken UX.",
    production_path: "Move to a server-side ASR (Groq Whisper) with proper VAD. Then the 'how long is too long a silence' decision becomes a tunable backend config, not a browser quirk.",
  },
  {
    id: "bars-vs-simple",
    decision: "Assessment scoring: BARS-anchored 5-dimension rubric",
    context: "Need a scoring system the LLM can apply consistently across candidates and that hiring managers can actually defend in a calibration meeting.",
    options_considered: ["Single overall 1–10 score", "5 dimensions with one-line descriptions", "BARS with level 1/3/5 behavioural anchors", "Pairwise comparison against gold-standard transcripts"],
    chosen: "BARS with level 1/3/5 anchors",
    reasoning: "Behaviorally Anchored Rating Scales come from industrial-organisational psychology. A level-5 patience anchor isn't 'very patient', it's 'explicitly diagnoses why the student is stuck before re-explaining, normalises struggle'. That gives the LLM a concrete target to compare against. Without anchors, GPT-class models score on vibes and identical answers can score 3/5 or 5/5 depending on phrasing. With anchors, scoring is defensibly reproducible. Cost: ~150 tokens of prompt, well within budget.",
    tradeoff: "Longer system prompt (closer to the token budget). Anchors are still subjective — two LLM calls on the same transcript can disagree by ±0.5 on a single dimension. And the anchors are written by me, so they encode my biases about what 'good teaching' looks like.",
    production_path: "Have actual Cuemath senior tutors write the anchors instead of me. Calibrate by running 100 historical interviews through the system and comparing AI scores to human scores. Tune anchor wording until correlation > 0.8.",
  },
  {
    id: "data-storage-strategy",
    decision: "Data layer: hand-seeded data + localStorage (no Supabase, no Firebase)",
    context: "The admin dashboard needs to show realistic data on first load, and real interviews need to persist across refreshes.",
    options_considered: ["Supabase (free tier)", "Firebase (free tier)", "localStorage + seeded fixtures", "In-memory only"],
    chosen: "localStorage + 5 seeded story-telling fixtures",
    reasoning: "Supabase or Firebase would have meant adding auth, schema design, RLS rules, and a network round-trip on every dashboard load — for a demo that's only ever opened by the evaluator. localStorage gives me persistence across refreshes for free. The 5 seeded candidates (3 pass, 1 review, 1 reject) mean the very first load of /admin tells a complete story without needing the evaluator to run an interview. Real interviews are merged on top of the seed data, so the demo has both 'fake compelling examples' and 'real proof it works'.",
    tradeoff: "Data is per-browser. Two evaluators won't see each other's interviews. Clearing browser storage wipes everything. There's no audit trail.",
    production_path: "Supabase with a row-level-security policy that scopes interviews to the recruiter's org. Migration is straightforward because the data shape in localStorage already matches what the DB schema would be.",
  },
  {
    id: "report-storage",
    decision: "Report storage: localStorage keyed by UUID (no DB)",
    context: "After the interview, the report needs to be reachable via a shareable URL that survives refresh.",
    options_considered: ["localStorage + UUID URLs", "Server-side DB", "Sign a JWT into the URL containing the report"],
    chosen: "localStorage + UUID URLs",
    reasoning: "Same logic as the dashboard storage decision — zero infra, immediate to ship, the URL pattern (/report/abc123) is identical to what a DB-backed solution would produce. crypto.randomUUID() is browser-native, no library needed. When loading a report, I check seed candidates first, then localStorage; the empty state cleanly distinguishes 'no report yet' from 'report link expired'.",
    tradeoff: "Report only readable on the browser that created it. Forwarding the URL to HR doesn't actually share the data — they'd see the 'report link expired' state.",
    production_path: "Backend stores the report on assessment, returns the UUID. Frontend GET /api/reports/:id loads it. Same URL pattern, real persistence. Day-one project.",
  },
  {
    id: "hush-decision",
    decision: "Hush noise-suppression model: research, evaluate, reject for MVP",
    context: "Real-world tutor candidates often interview from noisy homes. Hush is an open-source background-speaker-removal model that would in principle help.",
    options_considered: ["Don't research it", "Research and ship", "Research and write up the analysis, then reject"],
    chosen: "Research and write up the analysis, then reject",
    reasoning: "I read the model card, the architecture (DeepFilterNet3 + speaker-separation head, ~8MB ONNX), and worked out two integration paths. Path A (browser-side ONNX) is blocked because Chrome's SpeechRecognition is a closed pipeline you cannot inject pre-processed audio into. Path B (replace the recognizer entirely with MediaRecorder + Groq Whisper + Hush server-side) is architecturally clean but represents 2–3 days of new work — a full pipeline rewrite. The MVP demo runs in a quiet room; Hush would buy nothing for the evaluator while risking the demo. Documented the whole analysis in docs/hush-model-analysis.md so it's clear this is a deliberate choice, not ignorance.",
    tradeoff: "We ship a Chrome-only, no-noise-suppression solution. In a real noisy environment some candidates will get worse transcripts than they deserve.",
    production_path: "Next-iteration hardening: rip out Web Speech API entirely, build the MediaRecorder + Hush + Groq Whisper pipeline described in the analysis doc. Unlocks every browser and gives us full audio control.",
  },
  {
    id: "stack-react-fastapi",
    decision: "Stack: React + Vite frontend, FastAPI backend (not Next.js)",
    context: "Need a fast iteration loop for the frontend and a clean API layer for the LLM calls.",
    options_considered: ["React + Vite + FastAPI", "Next.js full-stack with API routes", "SvelteKit", "Pure HTML/JS + FastAPI"],
    chosen: "React + Vite + FastAPI",
    reasoning: "FastAPI is the right answer for a Python LLM backend — Pydantic v2 gives me free validation, automatic docs, and the async story is excellent. Splitting frontend from backend means each plays to its strengths and the API contract is explicit rather than file-system-magic'd. Next.js full-stack would have mixed Python and JS concerns — FastAPI is also better suited to streaming Groq responses if I ever need to.",
    tradeoff: "Two folders worth of tooling, two CORS gotchas. With Next.js I could have everything in one repo.",
    production_path: "Same stack scales fine. Add a monorepo tool (pnpm workspaces or Turborepo) so the two folders share types via a /shared package. Add Sentry on both.",
  },
  {
    id: "no-admin-auth",
    decision: "No authentication on /admin (deliberate demo decision)",
    context: "An admin dashboard normally needs login. For a 5-day demo with one evaluator, login is friction with no security upside.",
    options_considered: ["Supabase Auth", "Custom JWT", "Hardcoded password", "No auth + sticky AdminBanner explaining why"],
    chosen: "No auth + sticky AdminBanner explaining why",
    reasoning: "Adding auth to a demo would add 4 hours of work for zero evaluator benefit. The recruiter wants to see the dashboard; a login screen is a hurdle in front of the actual product. The AdminBanner ('login is skipped for the demo, this would be gated in production') signals the intent — that's the engineering judgement: explicit, documented, deliberately incomplete.",
    tradeoff: "Anyone with the URL can see the dashboard. In a real product this would be unacceptable. Without the banner this would also look amateur — the banner is doing a lot of the trust work here.",
    production_path: "Supabase Auth with email magic links scoped to @cuemath.com domains. Roughly half a day's work — would be priority #1 on day one of a real funded build.",
  },
  {
    id: "token-budget-strategy",
    decision: "Token budget: hard 'last 4 messages only' window for /api/chat",
    context: "Groq free tier is 6,000 TPM. Every interview turn must stay under ~800 tokens total to stay safe even if multiple users hit the system in the same minute.",
    options_considered: ["Send full history (let Groq truncate)", "Last N messages where N is dynamic", "Last 4 messages, hard rule", "Summarise older turns into a running summary"],
    chosen: "Last 4 messages, hard rule",
    reasoning: "Predictability beats cleverness. With 'last 4', I know per-call token usage is bounded: 300-token system prompt + 4 messages averaging 80 tokens + 150-token response = ~770 tokens. Always. Dynamic windowing or running summaries add complexity and an extra LLM call per turn. The 7-turn interview structure is short enough that 'short-term memory' is fine — Priya doesn't need to remember turn 1 to ask a good turn 5 question because each turn has its own pre-defined diagnostic purpose anyway.",
    tradeoff: "Priya can't reference something the candidate said 5 turns ago. Some natural-conversation moments are lost.",
    production_path: "Add a running summary stored in the request payload (frontend updates it, backend never stores). Each turn the summary gets refreshed from the previous summary + latest exchange. Adds maybe 100 tokens per call but unlocks long-context references.",
  },
  {
    id: "json-parse-retry",
    decision: "JSON parsing: strip-and-retry once on assessment endpoint",
    context: "LLMs sometimes wrap JSON in ```json fences, prepend 'Sure, here is the assessment:', or trail off mid-object.",
    options_considered: ["Trust the JSON, fail loudly on parse error", "Use a JSON-mode-only model", "Strip + retry once", "Ask the LLM to validate its own output"],
    chosen: "Strip + retry once",
    reasoning: "First attempt: regular ASSESSMENT_SYSTEM_PROMPT, strip everything outside the outermost {} braces, json.loads. If that fails, second attempt with a stricter 'Return ONLY the JSON object, no markdown, no preamble' prompt. If that also fails, surface a real 500. This converts ~95% of 'soft' parse failures into successes and surfaces only genuine model failures. JSON mode would be cleaner but isn't reliably available on Groq's llama-3.3.",
    tradeoff: "Adds a second Groq call (and ~2s latency) on the unhappy path. Doubles the token cost when it triggers.",
    production_path: "Use a model with native JSON mode (GPT-4o, Claude with structured outputs) so the retry path becomes a fallback, not the norm. Or schema-validate with Pydantic and re-prompt with the validation error attached.",
  },
];
